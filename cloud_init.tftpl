#cloud-config
disable_root: true
user:
  name: peon
  lock_passwd: true
  shell: /bin/bash
  groups: wheel
  sudo: 
    - ALL=(ALL) NOPASSWD:ALL
  ssh_authorized_keys:
    %{ for ssh_pub_key in ssh_pub_keys ~}
    - ${ssh_pub_key}
    %{ endfor ~}

